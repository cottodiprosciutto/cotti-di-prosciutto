{
  "name": "cdp-prosciutti-cloud",
  "private": true,
  "version": "2.0.0",
  "description": "Dashboard CDP Taglio + Vaschetta con Supabase, immagini e modalità amministratore locale",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build:standalone": "node scripts/build-standalone.js",
    "build": "npm run build:standalone && npm test"
  }
}
